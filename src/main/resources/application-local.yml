spring:
  config:
    activate:
      on-profile: "local"
  cloud:
    kubernetes:
      config:
        enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration